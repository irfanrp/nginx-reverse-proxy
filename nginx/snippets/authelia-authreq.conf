# Gating Authelia. Dipasang di dalam location / yang mau diproteksi.
# Butuh snippet authelia-authz-location.conf di server yang sama.
auth_request /internal/authelia/authz;

# Teruskan identitas dari Authelia ke upstream (opsional dipakai app).
auth_request_set $user   $upstream_http_remote_user;
auth_request_set $groups $upstream_http_remote_groups;
auth_request_set $name   $upstream_http_remote_name;
auth_request_set $email  $upstream_http_remote_email;
proxy_set_header Remote-User   $user;
proxy_set_header Remote-Groups $groups;
proxy_set_header Remote-Name   $name;
proxy_set_header Remote-Email  $email;

# Belum login -> lempar ke portal, balik lagi ke URL semula.
error_page 401 =302 https://auth.aboutdevops.my.id/?rd=$scheme://$host$request_uri;
