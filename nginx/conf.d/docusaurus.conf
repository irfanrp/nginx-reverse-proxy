# HTTP -> HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name docs.aboutdevops.my.id;
    include /etc/nginx/snippets/redirect-https.conf;
}

# HTTPS - docs.aboutdevops.my.id -> Docusaurus
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name docs.aboutdevops.my.id;

    include /etc/nginx/snippets/ssl.conf;
    include /etc/nginx/snippets/security-headers.conf;

    location / {
        proxy_pass http://docs-site:3000;
        include /etc/nginx/snippets/proxy-params.conf;
    }
}
